{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter wardrobe-pwa run build && cp -r artifacts/wardrobe-pwa/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile"
}
