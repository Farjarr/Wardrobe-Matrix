{
  "name": "wardrobe-matrix",
  "private": true,
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/wardrobe-pwa run build"
  }
}
