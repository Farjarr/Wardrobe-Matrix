{
  "buildCommand": "ls -la && ls -la artifacts && cd artifacts/wardrobe-pwa && PORT=3000 BASE_PATH=/ pnpm run build",
  "outputDirectory": "artifacts/wardrobe-pwa/dist/public",
  "installCommand": "pnpm install --frozen-lockfile"
}
